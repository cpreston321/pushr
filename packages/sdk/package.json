{
  "name": "@pushr/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "pushr — minimal TypeScript SDK for sending notifications",
  "type": "module",
  "main": "index.ts",
  "types": "index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "build": "bun build index.ts --outdir=../../dist/sdk --target=node --format=esm --minify",
    "lint": "oxlint .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check ."
  }
}
